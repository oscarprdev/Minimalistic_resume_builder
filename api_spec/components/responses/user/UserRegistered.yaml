description: Returns user registered response
content:
  application/json:
    schema:
      $ref: 'components/_index.yaml#/schemas/CommonPostResponse'
      example:
        status: 201
        message: 'User is successfully registered'
